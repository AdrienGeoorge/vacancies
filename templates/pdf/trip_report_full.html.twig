<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{{ trip.name }} - {{ app_name }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            margin: 8mm 0;
        }

        body {
            font-family: 'DejaVu Sans', Arial, sans-serif;
            color: #1a1a1a;
            font-size: 11pt;
            line-height: 1.5;
        }

        .page-footer {
            position: fixed;
            bottom: -5mm;
            left: 0;
            right: 0;
            height: 13mm;
            text-align: center;
            padding: 2mm 10mm;
            border-top: 1px solid #e5e7eb;
            background: white;
        }

        .logo-container {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            justify-content: center;
        }

        .logo {
            width: 30px;
            height: 30px;
        }

        .app-name {
            font-size: 14pt;
            font-weight: 700;
            color: #44B8A7;
        }

        .generate-date {
            font-size: 9pt;
            color: #6b7280;
            display: block;
            margin-top: -2mm;
        }

        .cover-page {
            text-align: center;
            padding: 30mm 0;
        }

        .cover-image {
            width: 100%;
            max-width: 150mm;
            height: auto;
            border-radius: 75px;
            margin-bottom: 10mm;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .trip-title {
            font-size: 32pt;
            font-weight: 700;
            color: #1a1a1a;
        }

        .trip-destination {
            margin-top: -4mm;
            font-size: 18pt;
            color: #44B8A7;
            margin-bottom: 8mm;
        }

        .trip-dates {
            font-size: 14pt;
            color: #4b5563;
            margin-bottom: 1mm;
        }

        .trip-duration {
            font-size: 10pt;
            color: #fff;
            font-weight: 600;
            background: #44B8A7;
            display: inline-block;
            padding: 6px 12px;
            border-radius: 30px;
        }

        .trip-description {
            margin-top: 10mm;
            font-size: 12pt;
            color: #374151;
            max-width: 140mm;
            margin-left: auto;
            margin-right: auto;
            text-align: center;
        }

        /* Section headings */
        .section-title {
            text-align: center;
            font-size: 17pt;
            font-weight: 700;
            color: #1a1a1a;
            margin-bottom: 3mm;
        }

        /* Budget section */
        .budget-line {
            text-align: center;
            margin: 8mm 0;
        }

        .budget-detail {
            display: inline-block;
            font-size: 10pt;
            margin: 0 2mm;
            padding: 6px 12px;
            border-radius: 30px;
        }

        .budget-to-pay {
            background-color: #FBE5E6;
            border: 1px solid rgba(235, 62, 91, 0.2);
            color: #EB3E5B;
        }

        .budget-paid {
            background-color: #F9FAFB;
            border: 1px solid rgba(108, 113, 128, 0.2);
            color: #4C5463;
        }

        .budget-table {
            width: 80%;
            margin: 0 auto 10mm auto;
            border-collapse: collapse;
        }

        .budget-table th {
            background: #44B8A7;
            color: white;
            padding: 10px 15px;
            text-align: left;
            font-weight: 600;
        }

        .budget-table th:last-child {
            text-align: right;
        }

        .budget-table td {
            padding: 8px 15px;
            border-bottom: 1px solid #e5e7eb;
        }

        .budget-table td:last-child {
            text-align: right;
            font-weight: 600;
        }

        .budget-table tr:nth-child(even) {
            background: #f9fafb;
        }

        .budget-table tfoot td {
            background: #d1fae5;
            font-weight: 700;
            font-size: 12pt;
            border-top: 2px solid #44B8A7;
            padding-top: 10px;
        }

        .budget-table small {
            font-size: 9pt;
            color: #6b7280;
            font-weight: normal;
        }

        /* Planning section */
        .planning-day {
            width: 80%;
            margin: 2mm auto;
        }

        .day-header {
            font-size: 13pt;
            font-weight: 700;
            color: #1a1a1a;
            margin-bottom: 4mm;
        }

        .event-card {
            background: #f9fafb;
            padding: 10px 15px;
            margin-bottom: 2.5mm;
            border-left: 4px solid #44B8A7;
        }

        .event-time-title {
            font-size: 11pt;
            font-weight: 700;
            color: #1f2937;
            margin-bottom: 3px;
        }

        .event-time {
            color: #44B8A7;
        }

        .event-category {
            font-size: 9pt;
            margin: 8px 0 5px 0;
        }

        .event-type {
            padding: 0 2.5mm 0.5mm 2.5mm;
            border-radius: 30px;
            color: white;
            display: inline-block;
        }

        .event-time-to-go {
            display: inline-block;
            color: #6b7280;
        }

        .event-description {
            font-size: 10pt;
            color: #4b5563;
        }

        /* Stats section */
        .stats-grid {
            display: table;
            width: 80%;
            margin: 0 auto 10mm auto;
        }

        .stat-item {
            display: table-row;
            page-break-inside: avoid;
        }

        .stat-label,
        .stat-value {
            display: table-cell;
            padding: 10px 15px;
            border-bottom: 1px solid #e5e7eb;
        }

        .stat-label {
            font-weight: 600;
            color: #374151;
            width: 40%;
        }

        .stat-value {
            text-align: right;
            color: #1f2937;
            font-size: 12pt;
        }

        /* Notes section */
        .notes-content {
            width: 80%;
            margin: auto;
            background: #fffbeb;
            padding: 15px 30px;
            border-left: 4px solid #f59e0b;
            border-radius: 6px;
            font-size: 10pt;
            line-height: 1.6;
        }

        .page-break {
            page-break-after: always;
        }

        .section-after-break {
            padding-top: 5mm;
        }
    </style>
</head>
<body>
<div class="page-footer">
    <div class="logo-container">
        <img src="{{ logoPath }}" alt="{{ app_name }}" class="logo">
        <span class="app-name">{{ app_name }}</span>
    </div>
    <span class="generate-date">Généré le {{ generatedAt|date('d/m/Y à H:i') }}</span>
</div>

<div class="content">
    <div class="cover-page">
        {% if trip.coverImage %}
            <img src="http://localhost:8000/{{ trip.coverImage }}" alt="{{ trip.name }}" class="cover-image">
        {% endif %}

        <h1 class="trip-title">{{ trip.name }}</h1>
        <p class="trip-destination">{{ trip.destination }}</p>

        <p class="trip-dates">
            Du {{ trip.departureDate|format_datetime(locale='fr',pattern="EEEE dd MMMM YYYY") }}
            au {{ trip.returnDate|format_datetime(locale='fr',pattern="EEEE dd MMMM YYYY") }}
        </p>

        {% if trip.duration %}
            <p class="trip-duration">{{ trip.duration }} jours</p>
        {% endif %}

        {% if trip.description %}
            <div class="trip-description">
                {{ trip.description }}
            </div>
        {% endif %}
    </div>

    <div class="page-break"></div>

    {% if trip.budget %}
        <h2 class="section-title">Budget du voyage</h2>

        <div class="budget-line">
            <p class="budget-detail budget-to-pay">
                <strong>Reste à payer :</strong> {{ trip.budget.toPay|number_format(2, ',', ' ') }} €
            </p>
            <p class="budget-detail budget-paid">
                <strong>Payé :</strong> {{ trip.budget.paid|number_format(2, ',', ' ') }} €
            </p>
        </div>

        <table class="budget-table">
            <thead>
            <tr>
                <th>Catégorie</th>
                <th>Montant</th>
            </tr>
            </thead>
            <tbody>
            {% for category, data in trip.budget.details.reserved %}
                <tr>
                    <td>
                        {{ data.title }}
                        {% if data.description %}
                            <br><small>{{ data.description }}</small>
                        {% endif %}
                    </td>
                    <td>{{ data.amount|number_format(2, ',', ' ') }} €</td>
                </tr>
            {% endfor %}
            </tbody>
            <tfoot>
            <tr>
                <td><strong>TOTAL</strong></td>
                <td>{{ trip.budget.total|number_format(2, ',', ' ') }} €</td>
            </tr>
            </tfoot>
        </table>
    {% endif %}

    {% if trip.planning %}
        <div class="page-break"></div>

        <div class="section-after-break">
            <h2 class="section-title">Planning du voyage</h2>

            {% for day, events in trip.planning %}
                <div class="planning-day">
                    <h3 class="day-header">
                        {{ day|format_datetime(locale='fr',pattern="EEEE dd MMMM YYYY")|capitalize }}
                    </h3>

                    {% for event in events %}
                        <div class="event-card">
                            <div class="event-time-title">
                                <span class="event-time">{{ event.start|date('H:i') }}
                                    {% if event.end %}
                                        à {{ event.end|date('H:i') }}
                                    {% endif %}
                                </span>
                                • {{ event.title }}
                            </div>
                            <div class="event-category">
                                <div class="event-type" style="background-color: {{ event.color }}">
                                    {{ event.type }}
                                </div>
                                {% if event.timeToGo %}
                                    <div class="event-time-to-go">
                                        • {{ event.timeToGo }} min. de trajet
                                    </div>
                                {% endif %}
                            </div>
                            {% if event.description %}
                                <div class="event-description">
                                    {{ event.description }}
                                </div>
                            {% endif %}
                        </div>
                    {% endfor %}
                </div>
            {% endfor %}
        </div>
    {% endif %}

    {% if trip.stats %}
        <div class="page-break"></div>

        <h2 class="section-title">Statistiques du voyage</h2>

        <div class="stats-grid">
            {% if trip.stats.days %}
                <div class="stat-item">
                    <div class="stat-label">Durée du voyage</div>
                    <div class="stat-value">{{ trip.stats.days }} jours</div>
                </div>
            {% endif %}

            {% if trip.stats.activities %}
                <div class="stat-item">
                    <div class="stat-label">Activités planifiées</div>
                    <div class="stat-value">{{ trip.stats.activities }}</div>
                </div>
            {% endif %}

            {% if trip.budget and trip.stats.days %}
                <div class="stat-item">
                    <div class="stat-label">Budget par jour</div>
                    <div class="stat-value">{{ (trip.budget.total / trip.stats.days)|number_format(2, ',', ' ') }}€
                    </div>
                </div>
            {% endif %}

            {% if trip.budget and trip.travelers %}
                <div class="stat-item">
                    <div class="stat-label">Budget par personne</div>
                    <div class="stat-value">{{ (trip.budget.total / trip.travelers|length)|number_format(2, ',', ' ') }}
                        €
                    </div>
                </div>
            {% endif %}
        </div>
    {% endif %}

    {% if trip.notes %}
        <div class="page-break"></div>

        <h2 class="section-title">Notes</h2>

        <div class="notes-content">
            {{ trip.notes|raw }}
        </div>
    {% endif %}
</div>
</body>
</html>
