{% extends 'trip/base.html.twig' %}

{% block bodyTrip %}
    <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4 mt-0 md:mt-6 xl:mt-0">
        <div class="shadow rounded-3xl bg-box dark:bg-box-dark py-3.5 px-6 flex flex-col justify-between gap-3  order-1 xl:order-none">
            <div class="header">
                <div class="mb-2 flex flex-row items-center gap-2">
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor"
                         class="size-6">
                        <path d="M7.5 3.375c0-1.036.84-1.875 1.875-1.875h.375a3.75 3.75 0 0 1 3.75 3.75v1.875C13.5 8.161 14.34 9 15.375 9h1.875A3.75 3.75 0 0 1 21 12.75v3.375C21 17.16 20.16 18 19.125 18h-9.75A1.875 1.875 0 0 1 7.5 16.125V3.375Z"></path>
                        <path d="M15 5.25a5.23 5.23 0 0 0-1.279-3.434 9.768 9.768 0 0 1 6.963 6.963A5.23 5.23 0 0 0 17.25 7.5h-1.875A.375.375 0 0 1 15 7.125V5.25ZM4.875 6H6v10.125A3.375 3.375 0 0 0 9.375 19.5H16.5v1.125c0 1.035-.84 1.875-1.875 1.875h-9.75A1.875 1.875 0 0 1 3 20.625V7.875C3 6.839 3.84 6 4.875 6Z"></path>
                    </svg>
                    <div class="font-poppins-semi-bold text-xl">Documents</div>
                </div>
                <div class="text-sm font-bold leading-4 mt-3.5">Ajoutez vos documents de voyage comme les réservations
                    de logements ou d'activités par exemple. Cliquez sur le nom d'un document pour le
                    consulter.
                </div>

                {% if not trip.documents.empty %}
                    <div class="flex flex-col overflow-y-scroll mt-3">
                        {% for document in trip.documents|sort((a, b) => a.id < b.id)|slice(0, 6) %}
                            <a href="{{ path('trip_documents_show', {'trip': trip.id, 'document': document.id}) }}"
                               target="_blank"
                               class="text-blue-500 text-ellipsis overflow-hidden whitespace-nowrap">
                                {{ document.name }}
                            </a>
                        {% endfor %}
                    </div>
                {% else %}
                    <div class="mt-4 mb-2 text-sm">Pour le moment vous n'avez déposé aucun document
                        pour ce voyage.
                    </div>
                {% endif %}
            </div>
            <div class="flex flex-col gap-2">
                <a href="{{ path('trip_documents_new', {'trip': trip.id}) }}" class="shadow rounded-2xl py-2.5 px-5 bg-teal-500
                        text-white font-bold text-center border-2 border-teal-400 hover:bg-teal-700 transition-all
                        duration-300 ease-in-out focus:shadow-outline focus:outline-none">
                    Ajouter un document
                </a>
                {% if not trip.documents.empty %}
                    <a href="{{ path('trip_documents_bag', {'trip': trip.id}) }}" class="shadow
                        rounded-2xl py-2.5 px-5 bg-gray-700 text-white border-gray-500 hover:bg-gray-800 font-bold
                        text-center border-2 dark:bg-white dark:text-gray-700 dark:border-gray-300 hover:dark:bg-gray-200
                        block">
                        Accéder aux documents
                    </a>
                {% endif %}
            </div>
        </div>
    </div>

    <div class="grid grid-cols-1 gap-4 mt-4">
        <div class="shadow rounded-3xl bg-box dark:bg-box-dark overflow-y-hidden">
            <div class="pt-3.5 px-6">
                <div class="mb-2 flex flex-row items-center gap-2">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5"
                         stroke="currentColor" class="size-6">
                        <path stroke-linecap="round" stroke-linejoin="round"
                              d="m16.862 4.487 1.687-1.688a1.875 1.875 0 1 1 2.652 2.652L10.582 16.07a4.5 4.5 0 0 1-1.897 1.13L6 18l.8-2.685a4.5 4.5 0 0 1 1.13-1.897l8.932-8.931Zm0 0L19.5 7.125M18 14v4.75A2.25 2.25 0 0 1 15.75 21H5.25A2.25 2.25 0 0 1 3 18.75V8.25A2.25 2.25 0 0 1 5.25 6H10"></path>
                    </svg>
                    <div class="font-poppins-semi-bold text-xl">Notes</div>
                </div>
                <div class="text-sm font-bold leading-4 mt-3.5 mb-4">
                    La sauvegarde de votre bloc-notes se fait de façon automatique dès la fin de votre saisie.
                </div>
            </div>
            <div class="editor-container pb-3.5 px-4">
                <div id="editor" style="height: 30em;" data-trip="{{ trip.id }}">{{ trip.blocNotes|raw }}</div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('showTrip') }}
{% endblock %}