{% extends 'trip/base.html.twig' %}

{% block bodyTrip %}
    <div id="share-modal-button" class="cursor-pointer shadow rounded-2xl py-2.5 px-5 bg-rose-400 text-white
                    w-full font-bold text-center m-auto border-2 border-rose-300 hover:bg-rose-500 mt-4
                    transition-all duration-300 ease-in-out focus:shadow-outline focus:outline-none">
        Partager le voyage
    </div>

    <div class="mt-4">
        {% if not trip.tripTravelers.empty %}
            <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4 mt-4">
                {% for traveler in trip.tripTravelers %}
                    {% set creator = traveler.invited and trip.traveler.completeName == traveler.invited.completeName %}
                    <div class="shadow rounded-3xl bg-box dark:bg-box-dark py-3.5 px-6 flex flex-col justify-between items-center gap-4">
                        <div class="footer flex flex-row gap-2 items-center justify-center mt-3">
                            {% if not creator and not traveler.invited %}
                                <a href="{{ path('trip_traveler_edit', {'trip': trip.id, 'traveler': traveler.id}) }}"
                                   class="block bg-blue-500 border-2 border-blue-400 text-white text-center py-2 px-5 rounded-full">Editer</a>
                            {% endif %}

                            {% if not creator and (not traveler.invited or trip.traveler.completeName == app.user.completeName) %}
                                <a href="{{ path('trip_traveler_delete', {'trip': trip.id, 'traveler': traveler.id}) }}"
                                   class="block bg-red-500 border-2 border-red-400 text-white text-center
                                      py-2 px-5 rounded-full">
                                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"
                                         fill="currentColor"
                                         class="size-6">
                                        <path fill-rule="evenodd"
                                              d="M16.5 4.478v.227a48.816 48.816 0 0 1 3.878.512.75.75 0 1 1-.256 1.478l-.209-.035-1.005 13.07a3 3 0 0 1-2.991 2.77H8.084a3 3 0 0 1-2.991-2.77L4.087 6.66l-.209.035a.75.75 0 0 1-.256-1.478A48.567 48.567 0 0 1 7.5 4.705v-.227c0-1.564 1.213-2.9 2.816-2.951a52.662 52.662 0 0 1 3.369 0c1.603.051 2.815 1.387 2.815 2.951Zm-6.136-1.452a51.196 51.196 0 0 1 3.273 0C14.39 3.05 15 3.684 15 4.478v.113a49.488 49.488 0 0 0-6 0v-.113c0-.794.609-1.428 1.364-1.452Zm-.355 5.945a.75.75 0 1 0-1.5.058l.347 9a.75.75 0 1 0 1.499-.058l-.346-9Zm5.48.058a.75.75 0 1 0-1.498-.058l-.347 9a.75.75 0 0 0 1.5.058l.345-9Z"
                                              clip-rule="evenodd"></path>
                                    </svg>
                                </a>
                            {% endif %}
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% else %}
            <div class="shadow rounded-3xl bg-red-400 text-white py-3.5 px-6">
                Aucun voyageur n'a été trouvé pour ce voyage.
            </div>
        {% endif %}
    </div>
{% endblock %}